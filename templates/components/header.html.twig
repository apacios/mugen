<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('dashboard_index') }}">Mugen Video</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% for category in getCategories() %}

                    {% if 'featured_film' == category.type %}
                        {% set path = 'featuredfilm_list' %}
                    {% elseif 'serie' == category.type %}
                         {% set path = 'serie_list' %}
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path(path, {id: category.id}) }}">
                            <span class="material-icons">{{ category.icon }}</span> {{ category.name }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>
